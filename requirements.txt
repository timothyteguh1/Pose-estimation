# Dependency langsung yang dipakai app.py & src/ (versi persis sesuai venv
# yang sudah divalidasi -- lihat `pip freeze` utk daftar lengkap termasuk
# dependency transitif).

# --- Ekstraksi pose, fitur, training ---
mediapipe==0.10.14
opencv-python==4.11.0.86
pandas==2.0.3
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.3.0

# --- Deteksi objek (YOLOv11) ---
ultralytics==8.4.138
# torch/torchvision versi CPU-only. Install manual (bukan lewat Dockerfile):
#   pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
torchvision==0.29.0

# --- Aplikasi web (client-server, bab 6.4 proposal) ---
streamlit==1.63.0
streamlit-webrtc==0.77.0
av==17.1.0

# protobuf dipaksa ke versi ini -- mediapipe minta <5, streamlit minta
# >=5.26.1, dua rentang itu tidak berpotongan. 4.25.9 terbukti jalan
# (dicek `pip freeze`); pip resolver default menolak kombinasi ini kalau
# dicek sekaligus, jadi dipasang eksplisit + --use-deprecated=legacy-resolver
# (lihat Dockerfile).
protobuf==4.25.9
